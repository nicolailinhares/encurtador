<!DOCTYPE html>
<html>
<head>
  <title>Encurtador</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<div>
	<h2 style="float:left">Encurtados de Link$</h2>
	<p style="float:right"><%= usuario_signed_in? ? (link_to "Sair", destroy_session_path(:usuario), :method => :delete)  : ((link_to "Entar", new_session_path(:usuario)) + " | " + (link_to "Criar contar", new_registration_path(:usuario))) %></p>
</div>
<br style="clear:both" />
<%= yield %>

</body>
</html>
